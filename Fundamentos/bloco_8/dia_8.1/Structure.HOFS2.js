const numberGenerator = () => {
  return Math.random() * 100;
}

console.log(numberGenerator); // Dessa forma apenas imprimimos a função

// console.log(numberGenerator()); // Dessa forma executamos a função.