const fetchPromise = () => {
  new Promise ((resolve, reject) => {
   const myArray = Array.from(
      {length: 10},
      () => Math.floor(Math.random() * 50) + 1,);
      myArray.map((element) => number * number);
  }
  ); 
}