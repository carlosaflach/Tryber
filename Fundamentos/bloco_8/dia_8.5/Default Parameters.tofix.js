const multiply = (number, value = 1) => {
  // Escreva aqui a sua função
  return number * value;
};

console.log(multiply(8));

// Nesse exemplo, se eu não declarar o value, o default será o número 1.
